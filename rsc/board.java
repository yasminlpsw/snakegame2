public class board {
}
import java.awt.*;
public class Board { no usages
    public void draw(Graphics2D g2) { no usages
        drawGrass (g2);
    }
    private void drawGrass (Graphics2D g2) { 1 usage
        int tile GameConstants.TILE;
        for (int x = 0 x < GameConstants.COLS; x++) {
            for (int y = 0 ; y < GameConstants.ROWS; y++) {
                boolean shade = (x)
            }
        }
    }
}
